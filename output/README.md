# Carpeta de salida

Aqui apareceran los archivos Markdown generados por la conversion.

Esta carpeta tambien puede incluir `conversion_report.txt` con el resumen de la
ultima ejecucion.

Los archivos generados en esta carpeta son ignorados por git (ver `.gitignore`).
